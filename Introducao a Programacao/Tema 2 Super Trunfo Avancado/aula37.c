#include <stdio.h>

int main() { // Operador NÃO (NOT) Lógico (!)

    int a = 10;

    if (!(a > 0)) {
        printf("O número é negativo.\n");
    } else {
        printf("O número é positivo.\n");
    }

}