#include <stdio.h>

int main() { // Operador E (AND) Lógico (&&)

    int a = 10, b = 5;

    if (a > 0 && b > 0) {
        printf("Os dois números são positivos.\n");
    } else {
        printf("Pelo menos um número é negativo.\n");
    }

}