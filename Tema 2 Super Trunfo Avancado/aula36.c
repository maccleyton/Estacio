#include <stdio.h>

int main() { // Operador OU (OR) Lógico (||)

    int a = -10, b = 5;

    if (a > 0 || b > 0) {
        printf("Pelo menos um número é positivo.\n");
    } else {
        printf("Os dois números são negativos.\n");
    }

}